---
title: Themes
---

# Themes

Set `data-theme` on the document root to select a theme:

```html
<html data-theme="light">
```

Supported values are `light` and `dark`. `ThemeSwitcher` can manage persistence and system preference for interactive applications.
