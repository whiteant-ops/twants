---
title: Accessibility
---

# Accessibility

Use semantic elements, provide visible labels, and preserve the default focus ring.

- Use `button` for actions and links for navigation.
- Give icon-only controls an accessible name.
- Keep keyboard focus visible.
- Pair status updates with an appropriate ARIA live region.
